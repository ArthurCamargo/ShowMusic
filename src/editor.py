
class Editor:
    """
    Class that manage the editor
        for composing the musics
    """
    def __init__(self, stream_of_char = '', cursor_pos=0, actual_char):
        self.stream_of_char = stream
        self.cursor_pos = cursor_pos
        self.actual_char = actual_char

